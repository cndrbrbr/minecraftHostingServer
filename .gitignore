# Private SSH keys — never commit these
keys/mc*/sftp_key
keys/mc*/ctrl_key

# Populated .env contains public keys (safe to commit if desired,
# but excluded here to avoid accidental exposure)
.env

# Generated by configure-memory.sh — machine-specific
start-mc*.sh
start-lobby.sh
start-bungee.sh

# Backup archives — never commit these
backups/
